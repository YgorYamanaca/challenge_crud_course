version: '3.8'

services:
  postgres:
    container_name: challenge_crud_curso
    image: postgres
    ports:
      - 5432:5432
    environment:
    - POSTGRES_USER=admin
    - POSTGRES_PASSWORD=admin
    - POSTGRES_DB=challenge_curso